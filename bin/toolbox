#!/bin/sh

# N.B.
# This file is intended for use in your development environment, and not
# production

set -e

RUST_LOG="psst=debug,toolbox=debug,reqwest=debug,serde=debug" bin/helpers/run-package toolbox "$@"
