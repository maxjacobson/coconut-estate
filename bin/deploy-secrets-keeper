#!/bin/sh

set -e

bin/toolbox provision secrets_keeper

exit 1 # TODO: remove
# TODO: re-enable this part
bin/production-build secrets_keeper
# And then after it's built, invoke another command which syncs the binary up
# and restarts the systemd service
